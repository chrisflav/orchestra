{
  "openapi": "3.1.0",
  "info": {
    "title": "orchestra dashboard API",
    "version": "1.0.0",
    "summary": "HTTP API over a running orchestra backend: reads everything it knows, and changes what it is configured with.",
    "description": "Everything the daemon knows about its queue, task history, listeners, projects and authentication sources, as JSON. Every read is also available as a Server-Sent Events stream at the same path under `/sse/v1/`, pushed every two seconds and only when the payload has changed.\n\nThree resources are configuration and are writable: **listeners**, **roles** and **skills**. Everything else — the queue, task history, concerts, projects, authentication sources — is a record of something that already happened or is owned by another system, and is read-only here. Nothing in this API enqueues work; that is the daemon's control socket. The one exception is `POST /api/v1/queue/{id}/cancel`, which stops one task the daemon is running \u2014 it writes nothing, and forwards a single message naming that entry to the socket, which stays off the network itself.\n\n## Writing\n\n- Every route that is not a `GET` requires a credential. `POST /api/login` is the only exception, since it is how one is obtained.\n- Every write must be sent as `application/json`. A request without that content type is a `415`. This is the second of the two locks on a cookie-authenticated write: the session cookie is `SameSite=Strict`, so a cross-site request carries no credential, and an HTML form cannot send JSON, so it cannot form the request at all.\n- `POST` to a collection creates and refuses to overwrite (`409`), taking the name from the body. `PUT` to a member creates or replaces, taking the name from the path; a body naming a different one is a `400`. Listeners are the exception and have no `POST`: a listener config carries no name of its own — its file names it — so `PUT /api/v1/listeners/{name}` is both how one is created and how it is replaced.\n- `DELETE` answers `204`, or `404` when there was nothing there.\n- A rejected body changes nothing on disk: validation runs to completion before anything is written, and every write is a write-and-rename, so a reader never sees a partial file.\n- Listener configs and roles are stored **verbatim**, not re-serialised. That is what keeps `{{secret}}` placeholders — and any field a client does not know about — intact across an edit. Fetch the `config` field of a detail response, change it, and `PUT` it back.\n\n## Conventions\n\n- Instants are RFC 3339 in UTC (`2026-07-23T10:04:11Z`) and live in a field named `...At`.\n- Durations are integer seconds, in a field named `...Seconds`.\n- An absent value is `null`, never an empty string.\n- Enumerations are the lower-case names the daemon itself uses.\n- Collections answer in a common envelope (`items`, `total`, `limit`, `offset`) and accept `limit`, `offset` and — where the collection is ordered by time — `since`.\n- A parameter that is malformed, or that the collection cannot honour, is a `400`. Parameters this version does not define are ignored.",
    "license": { "name": "See repository" }
  },
  "servers": [
    { "url": "http://127.0.0.1:8080", "description": "Default local bind" }
  ],
  "security": [{ "bearerAuth": [] }, { "sessionCookie": [] }],
  "tags": [
    {
      "name": "interactive",
      "description": "Chat sessions the daemon holds open. A session is a conversation with an agent, bound to a repository and running in the same sandbox with the same credentials and MCP tools a task would get. Unlike a task it stays up between turns: the clone slot, the MCP server and the agent process are acquired once and kept, so a turn costs a line on a pipe rather than a process start.\n\nThe four routes that do something rather than read something forward to the daemon over its control socket, which is not on the network. Each answers `409` when the daemon is not reachable or refuses — a statement about the daemon rather than about this server. The reads come off disk instead, so they answer whether the daemon is busy, restarting or gone."
    },
    { "name": "session", "description": "Acquiring and releasing a credential." },
    { "name": "queue", "description": "Work waiting, running, and the concerts it belongs to." },
    { "name": "tasks", "description": "Runs that happened, and their logs." },
    { "name": "listeners", "description": "Event sources that enqueue work." },
    { "name": "roles", "description": "Reusable task templates the dispatcher spawns from." },
    { "name": "skills", "description": "Markdown handed to every agent this instance launches." },
    { "name": "projects", "description": "Taxis-backed projects and their issue graphs." },
    { "name": "auth", "description": "Configured credentials, the usage limits they are under, and what they have spent." }
  ],
  "paths": {
    "/api/v1/interactive": {
      "get": {
        "tags": [
          "interactive"
        ],
        "summary": "Every session, newest first",
        "description": "Sessions the daemon is holding and sessions that have ended, in one list. Read off disk, so it answers whether the daemon is up or not.",
        "operationId": "listInteractiveSessions",
        "parameters": [
          {
            "$ref": "#/components/parameters/limit"
          },
          {
            "$ref": "#/components/parameters/offset"
          },
          {
            "$ref": "#/components/parameters/since"
          }
        ],
        "responses": {
          "200": {
            "description": "A page of sessions.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    {
                      "$ref": "#/components/schemas/Collection"
                    },
                    {
                      "type": "object",
                      "properties": {
                        "items": {
                          "type": "array",
                          "items": {
                            "$ref": "#/components/schemas/InteractiveSession"
                          }
                        }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          }
        }
      },
      "post": {
        "tags": [
          "interactive"
        ],
        "summary": "Start a session",
        "description": "Asks the daemon to clone the repository, mint a token, start an MCP server and launch an agent that stays up across turns, and answers with the session it created.\n\nThis is the one route in this API that starts an agent. It takes the same credential as every other write.\n\n`400` for a body that does not name both repositories, and for a backend whose CLI cannot host a session — that judgement is the daemon's, relayed, because only it knows which backends it was built with. `409` when the daemon is not reachable, when it is already holding as many sessions as it is configured to, or when the repository has no free clone slot.",
        "operationId": "startInteractiveSession",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InteractiveCreate"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The session that was started.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InteractiveSessionDetail"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "415": {
            "$ref": "#/components/responses/UnsupportedMediaType"
          }
        }
      }
    },
    "/api/v1/interactive/{id}": {
      "get": {
        "tags": [
          "interactive"
        ],
        "summary": "One session",
        "description": "Everything known about the session, but not its transcript — that is its own path, because it grows.",
        "operationId": "getInteractiveSession",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The session's id.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InteractiveSessionDetail"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          }
        }
      },
      "delete": {
        "tags": [
          "interactive"
        ],
        "summary": "End a session",
        "description": "Stops the agent and releases the clone slot, the MCP server and the process. The transcript is kept.\n\nA session that has already ended is `204` rather than `409`: ending something that has ended is a request that no longer applies, not one that failed. `409` when the daemon is not reachable.",
        "operationId": "endInteractiveSession",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The session's id.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The session is over."
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          }
        }
      }
    },
    "/api/v1/interactive/{id}/events": {
      "get": {
        "tags": [
          "interactive"
        ],
        "summary": "The transcript, from a cursor",
        "description": "What the conversation has said since `after`. A cursor rather than an offset because a transcript only grows at the end: a client that has read to seq 40 asks for what follows 40 and gets the same answer whatever has been appended since, where an offset would shift under it.\n\n`total` counts what is left after the cursor, so \"you are twelve behind\" needs no second request. The SSE stream at the same path under `/sse/v1/` is the same thing pushed: each frame carries only what follows the last one sent, and its `id` is the last seq in it, so an `EventSource` reconnect resumes from `Last-Event-ID` without seeing an event twice or missing one.",
        "operationId": "getInteractiveTranscript",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The session's id.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "after",
            "in": "query",
            "required": false,
            "description": "Return events whose seq is greater than this. Default 0.",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "default": 0
            }
          },
          {
            "$ref": "#/components/parameters/limit"
          }
        ],
        "responses": {
          "200": {
            "description": "A page of the transcript.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "total",
                    "limit",
                    "after"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/InteractiveEvent"
                      }
                    },
                    "total": {
                      "type": "integer",
                      "description": "How many events follow the cursor in total, before the window."
                    },
                    "limit": {
                      "type": "integer"
                    },
                    "after": {
                      "type": "integer",
                      "description": "The cursor this answered."
                    }
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          }
        }
      }
    },
    "/api/v1/interactive/{id}/messages": {
      "post": {
        "tags": [
          "interactive"
        ],
        "summary": "Post a turn",
        "description": "Sends what the person typed to the agent. Answers the seq the turn was written at, so a client can start reading from exactly there rather than from wherever the transcript happens to be by the time it asks.\n\nA turn posted to a `dormant` session wakes it first — an agent is started and the conversation resumed — so this is also how a session that was put down for being idle, or that outlived a daemon restart, comes back. That first turn takes as long as starting a session does, and is refused if the daemon is already holding as many awake sessions as it may.\n\n`409` when the session is already working on a turn — refused rather than queued, because the agent would take the second line as soon as it finished the first and a person who typed twice would get two answers in an order nobody chose. Interrupt it, or wait. Also `409` when the session has ended, or the daemon is not reachable.",
        "operationId": "postInteractiveTurn",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The session's id.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "text"
                ],
                "properties": {
                  "text": {
                    "type": "string",
                    "description": "The turn."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The turn was delivered.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "id",
                    "seq"
                  ],
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "seq": {
                      "type": "integer",
                      "description": "Where the turn landed in the transcript."
                    }
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/BadRequest"
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "415": {
            "$ref": "#/components/responses/UnsupportedMediaType"
          }
        }
      }
    },
    "/api/v1/interactive/{id}/interrupt": {
      "post": {
        "tags": [
          "interactive"
        ],
        "summary": "Abandon the turn in flight",
        "description": "Stops the turn the agent is working on without killing the session: the process, the clone and the conversation all survive, and the next turn can be posted straight away. The turn ends the way every turn ends, with a result on the stream.\n\nThe body carries nothing — the session is named by the path and there is no second argument to give. `409` when no turn is running, or the daemon is not reachable.",
        "operationId": "interruptInteractiveTurn",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The session's id.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "additionalProperties": false
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The interrupt was delivered.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "id"
                  ],
                  "properties": {
                    "id": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/Unauthorized"
          },
          "404": {
            "$ref": "#/components/responses/NotFound"
          },
          "409": {
            "$ref": "#/components/responses/Conflict"
          },
          "415": {
            "$ref": "#/components/responses/UnsupportedMediaType"
          }
        }
      }
    },
    "/api/openapi.json": {
      "get": {
        "tags": ["session"],
        "summary": "This document",
        "description": "Served without a credential and outside the version prefix: a client has to be able to discover what it is talking to before it can authenticate. Always the spec of the binary answering, not of a checkout.",
        "security": [],
        "operationId": "getOpenApi",
        "responses": {
          "200": {
            "description": "The OpenAPI description of this API.",
            "content": { "application/json": { "schema": { "type": "object" } } }
          }
        }
      }
    },
    "/api/session": {
      "get": {
        "tags": ["session"],
        "summary": "Whether the caller holds a valid credential",
        "description": "Reachable without one, deliberately: it is what a client asks on load to decide between logging in and proceeding, and it discloses nothing that trying any other endpoint would not.",
        "security": [],
        "operationId": "getSession",
        "responses": {
          "200": {
            "description": "The answer.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["authenticated"],
                  "properties": { "authenticated": { "type": "boolean" } }
                }
              }
            }
          }
        }
      }
    },
    "/api/login": {
      "post": {
        "tags": ["session"],
        "summary": "Exchange the password for a session cookie",
        "description": "For browsers. A script should send `Authorization: Bearer <password>` on each request instead and never call this.",
        "security": [],
        "operationId": "login",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["password"],
                "properties": { "password": { "type": "string" } }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Accepted. The session id comes back in an `HttpOnly`, `SameSite=Strict` cookie.",
            "headers": {
              "Set-Cookie": {
                "description": "`orchestra_session=<id>; HttpOnly; SameSite=Strict; Path=/; Max-Age=<ttl>`",
                "schema": { "type": "string" }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["authenticated"],
                  "properties": { "authenticated": { "type": "boolean", "const": true } }
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/logout": {
      "post": {
        "tags": ["session"],
        "summary": "Revoke the caller's session",
        "description": "Reachable without a credential, like `POST /api/login`: revoking a session one may no longer hold is not a privileged act, and requiring a valid one would mean an expired session could not be cleared.",
        "security": [],
        "operationId": "logout",
        "responses": {
          "200": {
            "description": "Revoked, and the cookie cleared. Sessions live in memory, so a restart revokes every session anyway.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["authenticated"],
                  "properties": { "authenticated": { "type": "boolean", "const": false } }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/overview": {
      "get": {
        "tags": ["queue"],
        "summary": "One-request summary of the whole daemon",
        "description": "An aggregate, not a resource: counts plus two short projections, so a dashboard can render a first screen — and keep it current over one SSE stream — without fanning out. Everything in it is available in full from the collections below. It takes no parameters; the projections are deliberately fixed in size.",
        "operationId": "getOverview",
        "responses": {
          "200": {
            "description": "The summary.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Overview" } } }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/queue": {
      "get": {
        "tags": ["queue"],
        "summary": "Queue entries",
        "description": "Work waiting or running, newest first. Includes entries in terminal states that have not yet been cleared.",
        "operationId": "listQueue",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" },
          { "$ref": "#/components/parameters/since" }
        ],
        "responses": {
          "200": {
            "description": "A page of queue entries.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/QueueEntry" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/queue/{id}/cancel": {
      "post": {
        "tags": ["queue"],
        "summary": "Cancel one running task",
        "description": "Asks the queue daemon to stop the task this id names, and only that one. Everything else it is running carries on, the queue is untouched, and the entry lands as `cancelled` \u2014 which `orchestra queue retry` re-enqueues.\n\nThe id may be a queue entry's id or the id of the run it became; both are ids this API hands out for the same piece of work. The request carries no arguments beyond it \u2014 the body is an empty JSON object \u2014 because only the daemon holds the cancellation token, and naming the entry is the whole of what it needs.\n\n`404` if no entry and no run carries the id. `409` if it exists and is not running, naming the status it is in; `409` also if the daemon is not running or did not answer, which is a statement about the daemon rather than about this server. The status the entry ends in is written by the worker that was running it, as it lands, not here.",
        "operationId": "cancelTask",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "A queue entry id, or the id of the run it became.",
            "schema": { "type": "string" }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": { "type": "object", "additionalProperties": false }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The daemon accepted the request and cancelled that task.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["id", "taskId"],
                  "properties": {
                    "id": { "type": "string", "description": "The queue entry the id resolved to." },
                    "taskId": { "type": ["string", "null"], "description": "The run it became, if it had started one." }
                  }
                }
              }
            }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" },
          "409": {
            "description": "The task is not running, or the queue daemon is not.",
            "content": {
              "application/json": {
                "schema": { "$ref": "#/components/schemas/Error" }
              }
            }
          },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      }
    },
    "/api/v1/tasks": {
      "get": {
        "tags": ["tasks"],
        "summary": "Task history",
        "description": "Runs that happened, newest first.",
        "operationId": "listTasks",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" },
          { "$ref": "#/components/parameters/since" }
        ],
        "responses": {
          "200": {
            "description": "A page of tasks.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Task" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/tasks/{id}": {
      "get": {
        "tags": ["tasks"],
        "summary": "One task, with the tail of its log",
        "description": "Resolves against task history first and the queue second, so a run that is still in flight is addressable by the same id.",
        "operationId": "getTask",
        "parameters": [
          { "$ref": "#/components/parameters/id" },
          {
            "name": "logLimit",
            "in": "query",
            "description": "How many trailing log events to return.",
            "required": false,
            "schema": { "type": "integer", "minimum": 0, "maximum": 10000, "default": 500 }
          }
        ],
        "responses": {
          "200": {
            "description": "The task.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/TaskDetail" } } }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/concerts": {
      "get": {
        "tags": ["queue"],
        "summary": "Concert runs",
        "description": "Multi-step workflow runs, newest first. Ordered and filtered by `startedAt`.",
        "operationId": "listConcerts",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" },
          { "$ref": "#/components/parameters/since" }
        ],
        "responses": {
          "200": {
            "description": "A page of concert runs.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Concert" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/concerts/{id}": {
      "get": {
        "tags": ["queue"],
        "summary": "One concert run and its steps",
        "operationId": "getConcert",
        "parameters": [{ "$ref": "#/components/parameters/id" }],
        "responses": {
          "200": {
            "description": "The concert run.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["concert", "steps"],
                  "properties": {
                    "concert": { "$ref": "#/components/schemas/Concert" },
                    "steps": { "type": "array", "items": { "$ref": "#/components/schemas/QueueEntry" } }
                  }
                }
              }
            }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/listeners": {
      "get": {
        "tags": ["listeners"],
        "summary": "Configured listeners",
        "description": "Configuration rather than events, so this collection has no time order and does not accept `since`.",
        "operationId": "listListeners",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" }
        ],
        "responses": {
          "200": {
            "description": "A page of listeners.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Listener" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/listeners/{name}": {
      "get": {
        "tags": ["listeners"],
        "summary": "One listener, with its source and the task it enqueues",
        "operationId": "getListener",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "Listener name. Percent-encode it; a component that could name a path is rejected as not found.",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "The listener.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ListenerDetail" } } }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      },
      "put": {
        "tags": ["listeners"],
        "summary": "Create or replace a listener",
        "description": "The body is the listener config document itself, and the path names it: a listener is `<config>/listeners/<name>.json` and is known by that spelling everywhere. This is also how a listener is created \u2014 there is no `POST` to the collection, because the document has no name of its own to be created by.\n\nThe daemon re-reads listener configs on every tick and rescans the directory every fifteen seconds, so a listener written here starts polling without a restart.\n\nA body still carrying the `name` field listeners used to be identified by is accepted when it agrees with the path and a `400` when it does not: the field is ignored, and silently storing the document under a different name than it claims is how a listener ends up listed under one name and loadable under another.",
        "operationId": "putListener",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ListenerConfig" } } }
        },
        "responses": {
          "200": { "$ref": "#/components/responses/ListenerWritten" },
          "201": { "$ref": "#/components/responses/ListenerWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      },
      "delete": {
        "tags": ["listeners"],
        "summary": "Delete a listener",
        "description": "Removes the config and the state that belonged to it. The state is a list of event ids already handled, and keeping it would mean a listener re-created under the same name silently ignored every event its predecessor had seen.\n\nThe daemon's fiber for this listener retires within one tick of its config disappearing.",
        "operationId": "deleteListener",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "responses": {
          "204": { "$ref": "#/components/responses/Deleted" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/listeners/{name}/enabled": {
      "put": {
        "tags": ["listeners"],
        "summary": "Turn a listener on or off",
        "description": "A sub-resource rather than a field of the config, because `enabled` lives in the listener's *state* file: turning one off is an operational act and must not require \u2014 or risk \u2014 rewriting its configuration. Takes effect on the listener's next tick.",
        "operationId": "setListenerEnabled",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["enabled"],
                "properties": { "enabled": { "type": "boolean" } }
              }
            }
          }
        },
        "responses": {
          "200": { "$ref": "#/components/responses/ListenerWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      }
    },
    "/api/v1/roles": {
      "get": {
        "tags": ["roles"],
        "summary": "Global roles",
        "description": "The roles in `<config>/roles/`. A project may shadow one by name under its own directory; that copy belongs to the project and is not served or written here.\n\nConfiguration rather than events, so this collection has no time order and does not accept `since`.",
        "operationId": "listRoles",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" }
        ],
        "responses": {
          "200": {
            "description": "A page of roles, ordered by name.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Role" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      },
      "post": {
        "tags": ["roles"],
        "summary": "Create a role",
        "description": "The body is the role document itself, and the name comes from its `name` field. Refuses to overwrite.\n\n`permissions` is checked against the tool groups the sandbox actually grants (`create_pr`, `comment`, `label_issue`, `manage_issues`, `work_issues`, `review_issues`); a typo there is otherwise invisible until a dispatched agent finds itself without the tool it was told to use.",
        "operationId": "createRole",
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/RoleConfig" } } }
        },
        "responses": {
          "201": { "$ref": "#/components/responses/RoleWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "409": { "$ref": "#/components/responses/Conflict" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      }
    },
    "/api/v1/roles/{name}": {
      "get": {
        "tags": ["roles"],
        "summary": "One role, with its prompt template and the document behind it",
        "description": "A role file that exists but does not parse still answers, with `parseError` set and the derived fields absent \u2014 the point of a configuration API is to show the operator the file that needs fixing, not a `404` for something plainly there.",
        "operationId": "getRole",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "responses": {
          "200": {
            "description": "The role.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/RoleDetail" } } }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      },
      "put": {
        "tags": ["roles"],
        "summary": "Create or replace a role",
        "description": "The body is the role document itself; its `name` must equal the path component. Takes effect on the dispatcher's next tick \u2014 roles are read per dispatch, not cached.",
        "operationId": "putRole",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/RoleConfig" } } }
        },
        "responses": {
          "200": { "$ref": "#/components/responses/RoleWritten" },
          "201": { "$ref": "#/components/responses/RoleWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      },
      "delete": {
        "tags": ["roles"],
        "summary": "Delete a global role",
        "description": "A project-scoped role of the same name is left alone: it shadows the global one rather than extending it, so removing the global file does not remove the role from that project.",
        "operationId": "deleteRole",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "responses": {
          "204": { "$ref": "#/components/responses/Deleted" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/skills": {
      "get": {
        "tags": ["skills"],
        "summary": "Installed skills",
        "description": "The Markdown documents under `<config>/skills/skills/`, which every agent this instance launches is handed. Ordered by name.",
        "operationId": "listSkills",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" }
        ],
        "responses": {
          "200": {
            "description": "A page of skills.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Skill" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      },
      "post": {
        "tags": ["skills"],
        "summary": "Install a skill",
        "description": "A skill is Markdown, which a JSON body cannot be, so it travels wrapped: `{\"name\": \"\u2026\", \"content\": \"---\\nname: \u2026\"}`. Refuses to overwrite.",
        "operationId": "createSkill",
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/SkillWrite" } } }
        },
        "responses": {
          "201": { "$ref": "#/components/responses/SkillWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "409": { "$ref": "#/components/responses/Conflict" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      }
    },
    "/api/v1/skills/{name}": {
      "get": {
        "tags": ["skills"],
        "summary": "One skill, with its Markdown",
        "operationId": "getSkill",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "responses": {
          "200": {
            "description": "The skill.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/SkillDetail" } } }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      },
      "put": {
        "tags": ["skills"],
        "summary": "Install or replace a skill",
        "description": "The `content` must open with a YAML front matter block carrying a `name` matching the path component and a non-empty `description`. Both are required rather than optional: an agent reads the description to decide whether a skill is relevant before it reads the body, so a file without one is loaded and then never used.\n\nTakes effect for tasks launched after the write. A task already running keeps the skills it started with.",
        "operationId": "putSkill",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "requestBody": {
          "required": true,
          "content": { "application/json": { "schema": { "$ref": "#/components/schemas/SkillWrite" } } }
        },
        "responses": {
          "200": { "$ref": "#/components/responses/SkillWritten" },
          "201": { "$ref": "#/components/responses/SkillWritten" },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "415": { "$ref": "#/components/responses/UnsupportedMediaType" }
        }
      },
      "delete": {
        "tags": ["skills"],
        "summary": "Remove a skill",
        "description": "Removes the whole skill directory, which is the unit the plugin layout defines: a skill's supporting files live beside its `SKILL.md`, and leaving them behind would leave something that is not a skill and not nothing.",
        "operationId": "deleteSkill",
        "parameters": [{ "$ref": "#/components/parameters/name" }],
        "responses": {
          "204": { "$ref": "#/components/responses/Deleted" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/projects": {
      "get": {
        "tags": ["projects"],
        "summary": "Projects in the taxis tracker",
        "description": "Configuration rather than events, so this collection does not accept `since`.",
        "operationId": "listProjects",
        "parameters": [
          { "$ref": "#/components/parameters/limit" },
          { "$ref": "#/components/parameters/offset" }
        ],
        "responses": {
          "200": {
            "description": "A page of projects.",
            "content": {
              "application/json": {
                "schema": {
                  "allOf": [
                    { "$ref": "#/components/schemas/Collection" },
                    {
                      "type": "object",
                      "properties": {
                        "items": { "type": "array", "items": { "$ref": "#/components/schemas/Project" } }
                      }
                    }
                  ]
                }
              }
            }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "500": {
            "description": "The tracker could not be reached. The body carries what went wrong.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
          }
        }
      }
    },
    "/api/v1/projects/{id}": {
      "get": {
        "tags": ["projects"],
        "summary": "One project and its issue dependency graph",
        "operationId": "getProject",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "Taxis project id. An id that is not an integer is not found rather than looked up.",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "The project.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["project", "issues"],
                  "properties": {
                    "project": { "$ref": "#/components/schemas/Project" },
                    "issues": { "type": "array", "items": { "$ref": "#/components/schemas/Issue" } }
                  }
                }
              }
            }
          },
          "401": { "$ref": "#/components/responses/Unauthorized" },
          "404": { "$ref": "#/components/responses/NotFound" }
        }
      }
    },
    "/api/v1/auth": {
      "get": {
        "tags": ["auth"],
        "summary": "Configured authentication sources and their usage limits",
        "description": "Read from the usage store the daemon maintains, never polled live: the upstream endpoint meters requests, so a client that polled it every couple of seconds would rate-limit the very thing it is monitoring. `polledAt` on each source is how fresh the numbers are.\n\nOpen this when the queue has pending work and nothing is running.",
        "operationId": "getAuth",
        "responses": {
          "200": {
            "description": "The sources.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Auth" } } }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    },
    "/api/v1/usage": {
      "get": {
        "tags": ["auth"],
        "summary": "What each authentication source has spent, window by window",
        "description": "The history behind `/api/v1/auth`. Every poll is rolled up into the limit window it landed in, so a source reports one record per session window and one per week rather than one per poll: `peakPercent` is what that window consumed, and `open` marks the window still filling.\n\nOnly polls are recorded, and a window is only ever seen through the polls that caught it — `samples` says how many did. A source that is not polled (an API key, or a backend with `poll_usage: false`) has no history, which is an absence rather than a gap.",
        "operationId": "getUsage",
        "parameters": [
          {
            "name": "windows",
            "in": "query",
            "description": "How many of the most recent windows to return per series — a kind, and a model scope where it has one. Values above the maximum are clamped to it rather than rejected.",
            "required": false,
            "schema": { "type": "integer", "minimum": 0, "maximum": 240, "default": 60 }
          }
        ],
        "responses": {
          "200": {
            "description": "The history.",
            "content": { "application/json": { "schema": { "$ref": "#/components/schemas/UsageHistory" } } }
          },
          "400": { "$ref": "#/components/responses/BadRequest" },
          "401": { "$ref": "#/components/responses/Unauthorized" }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "description": "The dashboard password, sent as `Authorization: Bearer <password>`. This is the path for scripts. The password comes from `--password`, `$ORCHESTRA_DASHBOARD_PASSWORD`, or `<data>/dashboard.secret`."
      },
      "sessionCookie": {
        "type": "apiKey",
        "in": "cookie",
        "name": "orchestra_session",
        "description": "Issued by `POST /api/login`. `HttpOnly` and `SameSite=Strict`, so it also authenticates `EventSource` connections without putting a credential in a URL."
      }
    },
    "parameters": {
      "limit": {
        "name": "limit",
        "in": "query",
        "required": false,
        "description": "Maximum items to return. Values above the maximum are clamped to it rather than rejected.",
        "schema": { "type": "integer", "minimum": 0, "maximum": 500, "default": 50 }
      },
      "offset": {
        "name": "offset",
        "in": "query",
        "required": false,
        "description": "Items to skip. Compare against `total`, which counts matches before the window.",
        "schema": { "type": "integer", "minimum": 0, "default": 0 }
      },
      "since": {
        "name": "since",
        "in": "query",
        "required": false,
        "description": "Keep only items at or after this instant. Accepted only by collections ordered by time.",
        "schema": { "type": "string", "format": "date-time" }
      },
      "name": {
        "name": "name",
        "in": "path",
        "required": true,
        "description": "Configuration record name. Percent-encode it; a component that could name a path is rejected as not found.",
        "schema": { "type": "string" }
      },
      "id": {
        "name": "id",
        "in": "path",
        "required": true,
        "description": "Resource id. Percent-encode it; a component that could name a path is rejected as not found.",
        "schema": { "type": "string" }
      }
    },
    "responses": {
      "BadRequest": {
        "description": "A parameter was malformed, or is not one this endpoint accepts.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
      },
      "Unauthorized": {
        "description": "No credential, or one that is not valid.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
      },
      "NotFound": {
        "description": "No such resource.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
      },
      "Conflict": {
        "description": "A record of that name already exists. `POST` never overwrites; `PUT` does.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
      },
      "UnsupportedMediaType": {
        "description": "The write did not declare `application/json`. A deliberate lock, not a parser limitation \u2014 see the write conventions above.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/Error" } } }
      },
      "Deleted": {
        "description": "Deleted. No body, because there is nothing left to describe."
      },
      "ListenerWritten": {
        "description": "The listener as `GET /api/v1/listeners/{name}` would return it.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/ListenerDetail" } } }
      },
      "RoleWritten": {
        "description": "The role as `GET /api/v1/roles/{name}` would return it.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/RoleDetail" } } }
      },
      "SkillWritten": {
        "description": "The skill as `GET /api/v1/skills/{name}` would return it.",
        "content": { "application/json": { "schema": { "$ref": "#/components/schemas/SkillDetail" } } }
      }
    },
    "schemas": {
      "InteractiveStatus": {
        "type": "string",
        "enum": [
          "starting",
          "idle",
          "running",
          "dormant",
          "ended",
          "failed"
        ],
        "description": "Where the session is in its life. `dormant` is not an ending: nothing is running — the agent was stopped for being idle, or the daemon restarted — and the conversation is intact, so posting a turn starts an agent again and resumes it. `ended` and `failed` are the two terminal states and mean different things: a session that was closed or ran out of budget ended; one whose agent died did not."
      },
      "InteractiveSession": {
        "type": "object",
        "required": [
          "id",
          "status",
          "createdAt",
          "lastActivityAt",
          "endedAt",
          "upstream",
          "fork",
          "backend",
          "model",
          "turnCount",
          "costUsd",
          "lastEventSeq",
          "title",
          "error"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/InteractiveStatus"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "lastActivityAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session last did anything. What the idle timeout measures against."
          },
          "endedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "upstream": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "fork": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "backend": {
            "type": "string"
          },
          "model": {
            "type": [
              "string",
              "null"
            ]
          },
          "turnCount": {
            "type": "integer"
          },
          "costUsd": {
            "type": "number",
            "description": "Spend so far, as the agent reported it."
          },
          "lastEventSeq": {
            "type": "integer",
            "description": "The last seq written to the transcript. A client that has read this far is current."
          },
          "title": {
            "type": [
              "string",
              "null"
            ],
            "description": "The first thing the person said, truncated."
          },
          "error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it failed, when it did."
          }
        }
      },
      "InteractiveSessionDetail": {
        "type": "object",
        "required": [
          "id",
          "status",
          "createdAt",
          "lastActivityAt",
          "endedAt",
          "upstream",
          "fork",
          "backend",
          "model",
          "turnCount",
          "costUsd",
          "lastEventSeq",
          "title",
          "error",
          "budget",
          "slot",
          "agentSessionId",
          "resumedFrom"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/InteractiveStatus"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "lastActivityAt": {
            "type": "string",
            "format": "date-time",
            "description": "When the session last did anything. What the idle timeout measures against."
          },
          "endedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "upstream": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "fork": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "backend": {
            "type": "string"
          },
          "model": {
            "type": [
              "string",
              "null"
            ]
          },
          "turnCount": {
            "type": "integer"
          },
          "costUsd": {
            "type": "number",
            "description": "Spend so far, as the agent reported it."
          },
          "lastEventSeq": {
            "type": "integer",
            "description": "The last seq written to the transcript. A client that has read this far is current."
          },
          "title": {
            "type": [
              "string",
              "null"
            ],
            "description": "The first thing the person said, truncated."
          },
          "error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it failed, when it did."
          },
          "budget": {
            "type": "number",
            "description": "Maximum spend for the whole session, in USD — not for one turn."
          },
          "slot": {
            "type": "integer",
            "description": "The clone slot held for this session's lifetime."
          },
          "agentSessionId": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent CLI's own session id. Assigned before the process starts rather than read out of its stream, so a session whose agent died before saying anything can still be picked up."
          },
          "resumedFrom": {
            "type": [
              "string",
              "null"
            ],
            "description": "The session this one resumed, if any."
          }
        }
      },
      "InteractiveCreate": {
        "type": "object",
        "required": [
          "upstream",
          "fork"
        ],
        "properties": {
          "upstream": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "fork": {
            "type": "string",
            "description": "`owner/repo`."
          },
          "backend": {
            "type": "string",
            "description": "Agent backend. Only backends whose CLI can read turns from stdin can host a session; one that cannot is a `400` naming it."
          },
          "model": {
            "type": "string",
            "description": "Passed to the backend's own CLI untouched, so a family alias and a pinned id are equally valid; orchestra keeps no list of what is. Absent, or blank, runs the backend's default. Fixed for the session's whole life, because the agent process is launched once and kept."
          },
          "budget": {
            "type": "number",
            "description": "Maximum spend for the whole conversation, in USD — not per turn. Absent runs on the default of 20; more than 100 is a `400`. Reaching it ends the session rather than failing it, with a notice saying so and the transcript kept."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional MCP tools to grant. Omitted means all of them."
          },
          "systemPrompt": {
            "type": "string"
          },
          "resumeFrom": {
            "type": "string",
            "description": "Start this session by picking up the conversation another one was having. The old session is not revived; this is a new one that inherits its agent-side history. Checked as a session id: it becomes a directory name."
          }
        }
      },
      "InteractiveEvent": {
        "type": "object",
        "required": [
          "seq",
          "occurredAt",
          "kind"
        ],
        "description": "One line of the transcript. `kind` says what happened; the remaining fields depend on it. `agent` wraps an agent stream event unchanged, so a client that renders a task log renders a transcript. The other kinds carry what the agent's own stream cannot say: what the person typed, where a turn began and ended, and what the daemon did.",
        "properties": {
          "seq": {
            "type": "integer",
            "description": "Monotone from 1. The cursor `after` takes."
          },
          "occurredAt": {
            "type": "string",
            "format": "date-time"
          },
          "kind": {
            "type": "string",
            "enum": [
              "user",
              "agent",
              "turnStarted",
              "turnEnded",
              "notice"
            ]
          },
          "text": {
            "type": "string",
            "description": "`user`: what the person said."
          },
          "event": {
            "type": "object",
            "description": "`agent`: the agent stream event, unchanged."
          },
          "turn": {
            "type": "integer",
            "description": "`turnStarted`, `turnEnded`."
          },
          "subtype": {
            "type": "string",
            "description": "`turnEnded`: how the turn ended."
          },
          "costUsd": {
            "type": [
              "number",
              "null"
            ],
            "description": "`turnEnded`."
          },
          "durationSeconds": {
            "type": [
              "integer",
              "null"
            ],
            "description": "`turnEnded`."
          },
          "level": {
            "type": "string",
            "enum": [
              "info",
              "warning",
              "error"
            ],
            "description": "`notice`."
          },
          "message": {
            "type": "string",
            "description": "`notice`."
          }
        }
      },
      "Error": {
        "type": "object",
        "required": ["error"],
        "description": "The body of every non-2xx response.",
        "properties": { "error": { "type": "string", "description": "What went wrong, in one sentence." } }
      },
      "Collection": {
        "type": "object",
        "required": ["items", "total", "limit", "offset"],
        "description": "The envelope every collection answers in.",
        "properties": {
          "items": { "type": "array", "items": {} },
          "total": { "type": "integer", "description": "Matches before `limit` and `offset` are applied." },
          "limit": { "type": "integer", "description": "The window size actually used, after clamping." },
          "offset": { "type": "integer" }
        }
      },
      "QueueStatus": {
        "type": "string",
        "enum": ["pending", "running", "done", "failed", "unfinished", "cancelled"]
      },
      "TaskStatus": {
        "type": "string",
        "enum": ["running", "completed", "failed", "unfinished", "cancelled"]
      },
      "ConcertStatus": { "type": "string", "enum": ["running", "done", "failed", "cancelled"] },
      "IssueStatus": { "type": "string", "enum": ["open", "claimed", "completed", "abandoned"] },
      "QueueEntry": {
        "type": "object",
        "description": "Work waiting or running. Becomes a Task when it is claimed; `taskId` is that run.",
        "required": ["id", "status", "createdAt", "priority", "upstream", "fork", "prompt", "series", "backend", "model", "taskId", "concertId", "concertStepKey"],
        "properties": {
          "id": { "type": "string" },
          "status": { "$ref": "#/components/schemas/QueueStatus" },
          "createdAt": { "type": "string", "format": "date-time" },
          "priority": { "type": "integer", "description": "Higher runs first." },
          "upstream": { "type": ["string", "null"], "description": "`owner/repo` the work targets. Null for a repository-independent entry, which runs in a scratch workspace with nothing checked out." },
          "fork": { "type": ["string", "null"], "description": "`owner/repo` the agent pushes to. Null for a repository-independent entry." },
          "prompt": { "type": "string" },
          "series": { "type": ["string", "null"] },
          "backend": { "type": ["string", "null"], "description": "Agent backend, e.g. `claude`." },
          "model": { "type": ["string", "null"] },
          "taskId": { "type": ["string", "null"], "description": "The run this entry became, once claimed." },
          "concertId": { "type": ["string", "null"] },
          "concertStepKey": { "type": ["string", "null"] }
        }
      },
      "Task": {
        "type": "object",
        "description": "A run that happened.",
        "required": ["id", "status", "createdAt", "upstream", "fork", "prompt", "series", "backend", "model", "sessionId", "continuesFrom", "budgetUsd"],
        "properties": {
          "id": { "type": "string" },
          "status": { "$ref": "#/components/schemas/TaskStatus" },
          "createdAt": { "type": "string", "format": "date-time" },
          "upstream": { "type": ["string", "null"], "description": "Null for a repository-independent run, which ran in a scratch workspace with nothing checked out." },
          "fork": { "type": ["string", "null"], "description": "Null for a repository-independent run." },
          "prompt": { "type": "string" },
          "series": { "type": ["string", "null"] },
          "backend": { "type": ["string", "null"] },
          "model": { "type": ["string", "null"] },
          "sessionId": { "type": ["string", "null"] },
          "continuesFrom": { "type": ["string", "null"], "description": "Id of the run this one resumed." },
          "budgetUsd": { "type": ["number", "null"], "description": "Spend cap for the run." }
        }
      },
      "TaskDetail": {
        "type": "object",
        "required": ["id", "taskId", "status", "fork", "createdAt", "prompt", "log", "logTotal", "logLimit", "logTruncated"],
        "properties": {
          "id": { "type": "string" },
          "taskId": {
            "type": ["string", "null"],
            "description": "The run the log was read under. A queue entry and the task it becomes are numbered separately, so this is the record's own id when `id` names a task, the entry's run when it names an entry that has one, and null for an entry with no run — one still queued, or one that failed before it started."
          },
          "status": { "type": "string" },
          "fork": { "type": ["string", "null"], "description": "Null for a repository-independent run." },
          "createdAt": { "type": "string", "format": "date-time" },
          "prompt": { "type": "string" },
          "log": {
            "type": "array",
            "description": "The trailing `logLimit` events of the run's structured log, oldest first. Event shape is the agent backend's, not this API's, and is deliberately not constrained here.",
            "items": { "type": "object" }
          },
          "logTotal": { "type": "integer", "description": "Events in the log in total." },
          "logLimit": { "type": "integer", "description": "The tail size used." },
          "logTruncated": { "type": "boolean", "description": "Whether `log` is a tail rather than the whole." }
        }
      },
      "Concert": {
        "type": "object",
        "required": ["id", "status", "name", "workflowFile", "startedAt", "finishedAt"],
        "properties": {
          "id": { "type": "string" },
          "status": { "$ref": "#/components/schemas/ConcertStatus" },
          "name": { "type": ["string", "null"] },
          "workflowFile": { "type": ["string", "null"] },
          "startedAt": { "type": "string", "format": "date-time" },
          "finishedAt": { "type": ["string", "null"], "format": "date-time" }
        }
      },
      "Listener": {
        "type": "object",
        "required": ["name", "enabled", "sourceType", "intervalSeconds", "lastCheckedAt", "eventCount"],
        "properties": {
          "name": { "type": "string" },
          "enabled": { "type": "boolean" },
          "sourceType": { "type": "string", "description": "What the listener polls, e.g. `github-issues`." },
          "intervalSeconds": { "type": "integer", "description": "Poll interval." },
          "lastCheckedAt": { "type": ["string", "null"], "format": "date-time", "description": "`null` if it has never run." },
          "eventCount": { "type": "integer", "description": "Distinct events processed so far." }
        }
      },
      "ListenerConfig": {
        "type": "object",
        "required": ["source", "action"],
        "description": "A listener config document, exactly as stored at `<config>/listeners/<name>.json`. It carries no name of its own: the file names the listener, and that spelling keys its state, its routes here and every line the daemon logs about it. Stored verbatim rather than re-serialised, so `{{secret}}` placeholders and fields this version does not model both survive an edit; it is validated after secret substitution, which is how the daemon reads it. The `source` union and the `action` template are documented in the repository README under \"Listeners\".",
        "properties": {
          "source": { "type": "object", "description": "What to watch. `type` selects the variant: `github-issues`, `github-pr-reviews`, `github-comments`, `github-labels`, `github-label-count`, `shell`, `project-dispatcher`, `label-dispatcher`." },
          "action": { "type": "object", "description": "The task to enqueue when it fires. `mode` and `prompt_template` are required." },
          "interval_seconds": { "type": "integer", "minimum": 1, "default": 60, "description": "Poll interval. Zero is rejected: a listener polling zero seconds apart would spin against its source as fast as the network allows." }
        },
        "additionalProperties": true
      },
      "RoleConfig": {
        "type": "object",
        "required": ["name", "permissions", "prompt_template"],
        "description": "A role document, exactly as stored at `<config>/roles/<name>.json`. Stored verbatim.",
        "properties": {
          "name": { "type": "string", "description": "Must equal the name it is stored under." },
          "permissions": {
            "type": "array",
            "items": { "type": "string", "enum": ["create_pr", "comment", "label_issue", "manage_issues", "work_issues", "review_issues"] },
            "description": "Optional tool groups the agent is granted. The always-available tools are not listed here; naming them grants nothing."
          },
          "prompt_template": { "type": "string", "description": "The prompt body. Must not be empty \u2014 a role with nothing to say dispatches an agent with an empty prompt." },
          "backend": { "type": "string" },
          "model": { "type": "string" },
          "system_prompt": { "type": "string" },
          "prepend_prompt": { "type": "string" },
          "read_only": { "type": "boolean", "default": false },
          "priority": { "type": "integer", "default": 10 },
          "budget": { "type": "number", "description": "Maximum spend in USD." },
          "dispatch": { "$ref": "#/components/schemas/DispatchPolicy" }
        },
        "additionalProperties": true
      },
      "DispatchPolicy": {
        "type": "object",
        "nullable": true,
        "required": ["trigger", "max", "preClaim"],
        "description": "When the dispatcher may spawn this role automatically. Absent (`null`) means manual-spawn only.",
        "properties": {
          "trigger": { "type": "string", "enum": ["has_open_issues", "has_in_review_issues", "idle", "always"] },
          "max": { "type": "integer", "description": "Default cap when a dispatcher config omits one. `0` means auto-spawn is opt-in." },
          "preClaim": { "type": "boolean", "description": "For `has_open_issues` roles: claim the issue at spawn time." }
        }
      },
      "Role": {
        "type": "object",
        "required": ["name", "permissions", "backend", "model", "priority", "readOnly", "budgetUsd", "dispatch"],
        "description": "A global role, as the API renders it.",
        "properties": {
          "name": { "type": "string" },
          "permissions": { "type": "array", "items": { "type": "string" } },
          "backend": { "type": ["string", "null"] },
          "model": { "type": ["string", "null"] },
          "priority": { "type": "integer" },
          "readOnly": { "type": "boolean" },
          "budgetUsd": { "type": ["number", "null"] },
          "dispatch": { "$ref": "#/components/schemas/DispatchPolicy" }
        }
      },
      "RoleDetail": {
        "allOf": [
          { "$ref": "#/components/schemas/Role" },
          {
            "type": "object",
            "properties": {
              "parseError": { "type": ["string", "null"], "description": "Set when the file exists but is not a role. The derived fields above are then absent and only `name` and `config` are meaningful." },
              "systemPrompt": { "type": ["string", "null"] },
              "prependPrompt": { "type": ["string", "null"] },
              "promptTemplate": { "type": "string" },
              "config": { "description": "The document as stored. This is what to edit and `PUT` back.", "$ref": "#/components/schemas/RoleConfig" }
            }
          }
        ]
      },
      "Skill": {
        "type": "object",
        "required": ["name", "description", "updatedAt"],
        "description": "One skill: a Markdown document handed to every agent this instance launches.",
        "properties": {
          "name": { "type": "string", "description": "Directory name under `skills/`, and the front matter's `name`." },
          "description": { "type": ["string", "null"], "description": "The front matter's `description`. `null` only for a file put there by hand without one." },
          "updatedAt": { "type": ["string", "null"], "format": "date-time" }
        }
      },
      "SkillDetail": {
        "allOf": [
          { "$ref": "#/components/schemas/Skill" },
          {
            "type": "object",
            "required": ["content"],
            "properties": {
              "content": { "type": "string", "description": "The whole `SKILL.md`, front matter included." }
            }
          }
        ]
      },
      "SkillWrite": {
        "type": "object",
        "required": ["content"],
        "description": "A skill on the way in. Markdown cannot be a JSON body, so it travels wrapped.",
        "properties": {
          "name": { "type": "string", "description": "Required by `POST`, which names the skill from the body; ignored by `PUT`, which names it from the path." },
          "content": { "type": "string", "description": "The whole `SKILL.md`. Must open with a `---` front matter block carrying a `name` matching the path and a non-empty `description`." }
        }
      },
      "ListenerDetail": {
        "type": "object",
        "required": ["name", "enabled", "intervalSeconds", "lastCheckedAt", "eventCount", "sourceType", "sourceDetail", "sourceExtras", "action", "recentEvents"],
        "properties": {
          "name": { "type": "string" },
          "enabled": { "type": "boolean" },
          "intervalSeconds": { "type": "integer" },
          "lastCheckedAt": { "type": ["string", "null"], "format": "date-time" },
          "eventCount": { "type": "integer" },
          "sourceType": { "type": "string" },
          "sourceDetail": { "type": "string" },
          "sourceExtras": {
            "type": "array",
            "description": "Source-kind-specific fields, as `[label, value]` pairs.",
            "items": { "type": "array", "items": { "type": "string" }, "minItems": 2, "maxItems": 2 }
          },
          "action": { "$ref": "#/components/schemas/ListenerAction" },
          "recentEvents": {
            "type": "array",
            "description": "Ids of the most recently processed events, newest first.",
            "items": { "type": "string" }
          },
          "config": {
            "description": "The document as stored, `{{secret}}` placeholders unexpanded. Everything above it is derived and is what a display wants; this is what to edit and `PUT` back. `null` when the file could not be read.",
            "$ref": "#/components/schemas/ListenerConfig"
          }
        }
      },
      "ListenerAction": {
        "type": "object",
        "description": "The task a listener enqueues when its source matches.",
        "required": ["mode", "upstream", "fork", "series", "backend", "model", "workflowPath", "priority", "promptTemplate"],
        "properties": {
          "mode": { "type": "string", "enum": ["fork", "pr"] },
          "upstream": { "type": "string" },
          "fork": { "type": "string" },
          "series": { "type": ["string", "null"] },
          "backend": { "type": ["string", "null"] },
          "model": { "type": ["string", "null"] },
          "workflowPath": { "type": ["string", "null"], "description": "Set when the listener enqueues a whole concert." },
          "priority": { "type": "integer" },
          "promptTemplate": { "type": "string" }
        }
      },
      "IssueCounts": {
        "type": "object",
        "required": ["open", "claimed", "completed", "abandoned"],
        "properties": {
          "open": { "type": "integer" },
          "claimed": { "type": "integer" },
          "completed": { "type": "integer" },
          "abandoned": { "type": "integer" }
        }
      },
      "Project": {
        "type": "object",
        "required": ["id", "name", "description", "createdAt", "defaultTarget", "issueCount", "counts"],
        "properties": {
          "id": { "type": "string", "description": "Taxis id, as a string so it can be compared and put in a URL without numeric coercion." },
          "name": { "type": "string" },
          "description": { "type": ["string", "null"] },
          "createdAt": { "type": "string", "format": "date-time" },
          "defaultTarget": { "type": ["string", "null"], "description": "`owner/repo@branch` work lands on by default." },
          "issueCount": { "type": "integer" },
          "counts": { "$ref": "#/components/schemas/IssueCounts" }
        }
      },
      "Issue": {
        "type": "object",
        "description": "One node of a project's dependency graph.",
        "required": ["id", "title", "status", "parentId", "dependencies", "prCount", "claimedBy", "updatedAt"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "status": { "$ref": "#/components/schemas/IssueStatus" },
          "parentId": { "type": ["string", "null"] },
          "dependencies": { "type": "array", "items": { "type": "string" }, "description": "Issue ids that must finish first." },
          "prCount": { "type": "integer" },
          "claimedBy": { "type": ["string", "null"], "description": "Agent currently holding the claim." },
          "updatedAt": { "type": "string", "format": "date-time" }
        }
      },
      "UsageLimit": {
        "type": "object",
        "required": ["kind", "scope", "percent", "severity", "active", "resetsAt"],
        "properties": {
          "kind": { "type": "string", "description": "`session`, `weekly_all`, `weekly_scoped`, or whatever else upstream reports." },
          "scope": { "type": ["string", "null"], "description": "Model family a scoped limit applies to." },
          "percent": { "type": "integer", "minimum": 0 },
          "severity": { "type": "string", "enum": ["normal", "warning", "critical"] },
          "active": { "type": "boolean", "description": "Whether this limit is currently the binding one." },
          "resetsAt": { "type": ["string", "null"], "format": "date-time" }
        }
      },
      "AuthSource": {
        "type": "object",
        "required": ["label", "backend", "kind", "baseUrl", "isDefault", "pollable", "state", "reason", "availableAt", "pressure", "polledAt", "lastUsedAt", "lastError", "backoffUntil", "limits"],
        "properties": {
          "label": { "type": "string" },
          "backend": { "type": "string", "description": "Agent backend this source belongs to." },
          "kind": { "type": "string", "enum": ["oauth", "api-key"] },
          "baseUrl": { "type": ["string", "null"] },
          "isDefault": { "type": "boolean", "description": "What a task that names no source gets." },
          "pollable": { "type": "boolean", "description": "False for API-key sources, which bill per token and have no window to report." },
          "state": { "type": "string", "enum": ["available", "blocked"], "description": "Branch on this, not on `availableAt`." },
          "reason": { "type": ["string", "null"], "description": "Why it is blocked." },
          "availableAt": { "type": ["string", "null"], "format": "date-time", "description": "When the block lifts. `null` when available, and also when blocked with no reported reset." },
          "pressure": { "type": "number", "description": "Highest limit percentage across this source, as a fraction." },
          "polledAt": { "type": ["string", "null"], "format": "date-time", "description": "How fresh these numbers are." },
          "lastUsedAt": { "type": ["string", "null"], "format": "date-time" },
          "lastError": { "type": ["string", "null"] },
          "backoffUntil": { "type": ["string", "null"], "format": "date-time", "description": "Set only while a polling backoff is still in the future." },
          "limits": { "type": "array", "items": { "$ref": "#/components/schemas/UsageLimit" } }
        }
      },
      "AuthBackend": {
        "type": "object",
        "required": ["name", "defaultSource", "sources"],
        "properties": {
          "name": { "type": "string" },
          "defaultSource": { "type": ["string", "null"] },
          "sources": { "type": "array", "items": { "$ref": "#/components/schemas/AuthSource" } }
        }
      },
      "Auth": {
        "type": "object",
        "required": ["configError", "backends"],
        "properties": {
          "configError": { "type": ["string", "null"], "description": "Set instead of `backends` when the config could not be read." },
          "backends": { "type": "array", "items": { "$ref": "#/components/schemas/AuthBackend" } }
        }
      },
      "UsageWindow": {
        "type": "object",
        "description": "One session or weekly limit window, rolled up from every poll that landed inside it.",
        "required": ["kind", "scope", "startedAt", "updatedAt", "resetsAt", "peakPercent", "percent", "samples", "open"],
        "properties": {
          "kind": { "type": "string", "description": "`session`, `weekly_all`, `weekly_scoped`, or whatever else upstream reports." },
          "scope": { "type": ["string", "null"], "description": "Model family a scoped window applies to." },
          "startedAt": { "type": "string", "format": "date-time", "description": "The first poll that landed in this window — not the window's own start, which is only ever seen through the polls that caught it." },
          "updatedAt": { "type": "string", "format": "date-time", "description": "The last poll that landed in it." },
          "resetsAt": { "type": ["string", "null"], "format": "date-time", "description": "When the window resets, as every poll inside it reported." },
          "peakPercent": { "type": "integer", "minimum": 0, "description": "The highest reading. Utilisation only climbs inside a window, so this is what the window consumed." },
          "percent": { "type": "integer", "minimum": 0, "description": "The most recent reading. Usually equal to `peakPercent` on a closed window, though a poll that read low leaves it below." },
          "samples": { "type": "integer", "minimum": 0, "description": "How many polls saw this window. One is a glimpse of it rather than a measurement." },
          "open": { "type": "boolean", "description": "Whether this is the window still filling: the newest of its series, and not yet past its reset." }
        }
      },
      "UsageHistorySource": {
        "type": "object",
        "required": ["label", "backend", "kind", "pollable", "sessions", "weeks", "other"],
        "properties": {
          "label": { "type": "string" },
          "backend": { "type": "string" },
          "kind": { "type": "string", "enum": ["oauth", "api-key"] },
          "pollable": { "type": "boolean", "description": "False for API-key sources, which have no subscription window and so never accumulate history." },
          "sessions": { "type": "array", "description": "Session windows, oldest first.", "items": { "$ref": "#/components/schemas/UsageWindow" } },
          "weeks": { "type": "array", "description": "Weekly windows, both account-wide and model-scoped, oldest first.", "items": { "$ref": "#/components/schemas/UsageWindow" } },
          "other": { "type": "array", "description": "Windows of any other kind upstream reported, so that nothing recorded is unreachable.", "items": { "$ref": "#/components/schemas/UsageWindow" } }
        }
      },
      "UsageHistoryBackend": {
        "type": "object",
        "required": ["name", "sources"],
        "properties": {
          "name": { "type": "string" },
          "sources": { "type": "array", "items": { "$ref": "#/components/schemas/UsageHistorySource" } }
        }
      },
      "UsageHistory": {
        "type": "object",
        "required": ["configError", "backends"],
        "properties": {
          "configError": { "type": ["string", "null"], "description": "Set instead of `backends` when the config could not be read." },
          "backends": { "type": "array", "items": { "$ref": "#/components/schemas/UsageHistoryBackend" } }
        }
      },
      "OverviewCounts": {
        "type": "object",
        "required": ["running", "pending", "failed", "concerts", "listeners", "totalTasks", "authFree", "authTotal"],
        "properties": {
          "running": { "type": "integer" },
          "pending": { "type": "integer" },
          "failed": { "type": "integer" },
          "concerts": { "type": "integer", "description": "Concert runs currently running." },
          "listeners": { "type": "integer" },
          "totalTasks": { "type": "integer" },
          "authFree": { "type": "integer", "description": "Sources not currently blocked." },
          "authTotal": { "type": "integer" }
        }
      },
      "Overview": {
        "type": "object",
        "required": ["counts", "activeQueue", "recentTasks", "taxisUrl"],
        "properties": {
          "counts": { "$ref": "#/components/schemas/OverviewCounts" },
          "taxisUrl": {
            "type": ["string", "null"],
            "description": "Base URL of the configured taxis tracker, or `null` if none is configured. Projects live in taxis and are its business to display; this is what a client links out to instead of rendering them, and `null` means there is nothing to link to."
          },
          "activeQueue": {
            "type": "array",
            "description": "Running and pending entries. Not paged; use `/api/v1/queue` for the full list.",
            "items": { "$ref": "#/components/schemas/QueueEntry" }
          },
          "recentTasks": {
            "type": "array",
            "description": "The ten most recent runs. Use `/api/v1/tasks` for more.",
            "items": { "$ref": "#/components/schemas/Task" }
          }
        }
      }
    }
  },
  "x-sse": {
    "description": "Every path under `/api/v1/` that answers a `GET` is also a Server-Sent Events stream at the same path under `/sse/v1/`, carrying the identical JSON payload in the `data` field of each `message` event. Query parameters mean the same thing on both.\n\nFrames are pushed every two seconds, but only when the payload has changed since the last one, so an idle daemon costs nothing; a `: keep-alive` comment goes out every thirty seconds of silence to survive idle proxies. A stream whose resource disappears, or whose parameters are rejected, is closed rather than left hanging.\n\nAuthenticate with the session cookie or the bearer header, exactly as for the JSON routes.\n\nOne stream is not a repeated whole-document read: `/sse/v1/interactive/{id}/events` carries a cursor. A transcript only grows, so the whole payload is the whole conversation, and re-sending it every time a word is added is quadratic in the length of the chat. Each frame holds only what follows the last one sent, and its `id` is the last seq in it, so a browser's reconnect resumes from `Last-Event-ID` and anything else passes the same number as `after`. It also ticks faster than the rest, because chat latency is felt.",
    "example": "curl -N -H \"Authorization: Bearer $PASSWORD\" http://127.0.0.1:8080/sse/v1/overview"
  }
}
